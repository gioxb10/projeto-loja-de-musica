#include "mainwindow.h"
#include <string.h>

#include <QApplication>

using namespace std;

class GerenciadordeArquivo{

private:

public:
};

class Lojademusica{
    class GerenciadordeArquivo{
        private:
            std::string nomeDoArquivo;

        public:
  };
};
        class Cliente:GerenciadordeArquivo {
            private:
                std::string nome, endereco, numero;
            public:

        };

        class Guitarra:GerenciadordeArquivo
      {
            private:
                string nomeDaGuitarra, condicao, compradoPor;
                float valorDeEntrada, valorTotal;
            public:

        };

int main(int argc, char *argv[])
{
    QApplication a(argc, argv);
    MainWindow w;
    w.show();



    return a.exec();
}
